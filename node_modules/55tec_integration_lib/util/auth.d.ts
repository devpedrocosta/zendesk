import { Client as IntegrationClient } from '../model/protocol/integration';
import { Credential } from "../model/protocol/integrator/credential";
declare type Search = {
    credentials?: Credential[];
    clientId?: string;
};
export default class Auth {
    private integrationClient;
    constructor(integrationClient: IntegrationClient);
    static makeToken(clientId: string): string;
    getClient(search: Search): Promise<{
        clientId: string;
        sipServer: string;
    }>;
    getAuthToken(search: Search): Promise<string>;
}
export {};
